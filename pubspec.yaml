name: copyable
description: A Flutter package for giving and standardizing copy functionality to Dart classes.
version: 0.0.1+1
author: Juan Diego Fajardo <jfajardo@mit.edu>
homepage: https://github.com/kuanfajardo/copier.git

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.2.3
  # Generator
  analyzer: ^1.4.0
  build: ^2.1.0
  source_gen: ^1.0.0
  code_builder: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

